{
  "name": "api",
  "type": "module",
  "scripts": {
    "db:reset": "pnpm exec prisma migrate reset --force --skip-generate --skip-seed && pnpm exec prisma db seed",
    "dev": "pnpm exec tsx watch src/index.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.1",
    "@prisma/client": "6.6.0",
    "hono": "^4.7.7",
    "prisma": "6.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "tsx": "^4.7.1"
  }
}